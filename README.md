# ELEPHANT

## Deploy

Uses docker and docker-compose.
First you need to install:

`npm install`

Then build:

`npm run build`

Then compile docker images and run:

`docker-compose up`

## Development

### Install

`npm install`

### Run

`npm start`
